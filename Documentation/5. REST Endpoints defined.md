

## REST Endpoints

### Server defines REST endpoints

[urls.py](../fuel_optimizer/urls.py)
```
fuel_optimizer/urls.p
urlpatterns = [
    path("api/v1/route/", RouteView.as_view(), name="route"),
]

fuel_optimizer/views.py
from fuel_optimizer.services.route_service import RouteServiceError, plan_fuel_route
class RouteView(APIView):
    def post(self, request: Request) -> Response:   #For POST /api/v1/route/
        self._plan_route(serializer.validated_data["start"], serializer.validated_data["finish"])

    def get(self, request: Request) -> Response:    #For GET /api/v1/route/


    def _plan_route(self, start, finish) -> Response:
        payload = plan_fuel_route(start, finish)
```

#### Start the server
```
cd /home/amit/Fuel-Route-Optimizer
source .venv/bin/activate
python manage.py runserver
```

### Client hits the REST endpoint
POST
```
curl -s -X POST http://127.0.0.1:8000/api/v1/route/ \
  -H "Content-Type: application/json" \
  -d '{"start": "Chicago, IL", "finish": "Milwaukee, WI"}' \
  | python -m json.tool | grep -A6 '"summary"'
```

GET
```
curl -sG "http://127.0.0.1:8000/api/v1/route/" \
  --data-urlencode "start=Chicago, IL" \
  --data-urlencode "finish=Milwaukee, WI" \
  | python -m json.tool | grep -A6 '"summary"'

And get response
"summary": {
        "total_distance_miles": 92.8,
        "total_fuel_cost_usd": 0.0,
        "fuel_stops_count": 0,
        "mpg": 10,
        "max_range_miles": 500
    }
```